pipeline {
  ageny any
  
  environment {
    DEMO='1.3'
  }
  
  stages {
    stage('stage-1') {
      steps {
        echp "This is a build number $BUILD_NUMBER of demo $DEMO"
        sh '''
          echo "Using a multi-line shell step"
          chmod +x test.sh
          ./test/sh
        ```
        }
     }
  }
}
